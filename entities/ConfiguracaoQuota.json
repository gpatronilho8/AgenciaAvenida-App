{
  "name": "ConfiguracaoQuota",
  "type": "object",
  "properties": {
    "condominio_id": {
      "type": "string"
    },
    "tipo_calculo": {
      "type": "string",
      "enum": [
        "fixo",
        "permilagem"
      ],
      "default": "fixo",
      "description": "Quota fixa ou calculada por permilagem"
    },
    "valor_fixo": {
      "type": "number",
      "default": 0,
      "description": "Valor fixo por fração"
    },
    "valor_total_permilagem": {
      "type": "number",
      "default": 0,
      "description": "Valor total a distribuir por permilagem"
    },
    "dia_vencimento": {
      "type": "number",
      "description": "Dia do mês para vencimento"
    },
    "mes_inicio": {
      "type": "number",
      "description": "Mês de início de contagem (1-12)"
    },
    "ano_inicio": {
      "type": "number",
      "description": "Ano de início de contagem"
    },
    "ativa": {
      "type": "boolean",
      "default": true
    }
  },
  "required": [
    "condominio_id"
  ]
}